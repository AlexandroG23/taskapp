---

---

<!-- Modal para respuesta de creación y eliminación de tareas -->
<div
  id="task-modal"
  class="fixed inset-0 bg-black/30 backdrop-blur-sm z-50 hidden items-center justify-center"
>
  <div
    class="bg-white dark:bg-neutral-900 px-8 py-6 sm:py-8 sm:px-10 
           rounded-3xl shadow-2xl max-w-lg w-full 
           border border-gray-200 dark:border-neutral-700"
  >
    <h2
      id="task-modal-title"
      class="text-2xl font-bold mb-4 text-gray-900 dark:text-white"
    ></h2>
    <p
      id="task-modal-message"
      class="text-base text-gray-700 dark:text-gray-300 whitespace-pre-line break-words overflow-y-auto max-h-64 mb-6"
    ></p>

    <button
      id="task-modal-close"
      class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2.5 rounded-lg transition"
    >
      Cerrar
    </button>
  </div>
</div>
